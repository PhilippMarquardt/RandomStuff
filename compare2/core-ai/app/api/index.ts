// Export configuration
export * from './config';

// Export new services (recommended approach)
export * from './services';

// Legacy API modules are still available but must be imported explicitly
// e.g., import { sendMessage } from './api/chat'  
// New code should use the service objects instead 